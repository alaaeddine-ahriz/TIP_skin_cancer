# Ignore the images folder
images/
images-200k/
logs/
DataBase/

# Optional: Ignore any log files or temporary files in the project
*.log
*.tmp

# Optional: Ignore metadata files if not needed in the repository
images/combined_metadata.csv